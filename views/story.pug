extends layout

block content
    link(rel='stylesheet', href='/stylesheets/style.css')
    body(style=`background-color: ${color}; color: ${textColor}`)
    h1 My Story
    p Your Story: #{newStory}
    a(href='/') Try again